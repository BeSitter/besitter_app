name: Integration

on:
  push:
    branches: ["develop"]
  pull_request:
    branches: ["develop"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Building API
        uses: actions/setup-node@v3
        with:
          node-version: 14.x
          cache: "npm"
      - run: ls
